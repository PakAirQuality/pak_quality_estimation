# Feature engineering pipeline dependencies
# For GCP VM: pip install -r requirements.txt

# Core
numpy>=1.24.0
pandas>=2.0.0
scipy>=1.10.0
tqdm>=4.65.0
requests>=2.28.0
bottleneck>=1.3.0

# MET features (NetCDF/xarray/GRIB)
xarray>=2023.1.0
netCDF4>=1.6.0
h5py>=3.8.0
cfgrib>=0.9.10
eccodes>=1.5.0

# Geospatial (masking, boundaries)
geopandas>=0.14.0
regionmask>=0.10.0
shapely>=2.0.0

# AOD features (HDF4 MODIS files)
pyhdf>=0.10.5

# TROPOMI features (GeoTIFF)
rasterio>=1.3.0

# Parquet and GCS support
pyarrow>=14.0.0
gcsfs>=2023.1.0

# Training (if needed on VM)
scikit-learn>=1.3.0
lightgbm>=4.0.0

# Data ingestion (satellite data sources)
earthengine-api>=0.1.370  # Google Earth Engine for TROPOMI
earthaccess>=0.8.0  # NASA Earthdata for MODIS AOD
cdsapi>=0.6.0  # Copernicus CDS for ERA5
